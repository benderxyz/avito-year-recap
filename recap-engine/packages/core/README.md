# @recap-engine/core

Framework-agnostic foundation for year-recap: types, theme, player, `RecapPayload`, `prepareRecap`.

```bash
pnpm add @recap-engine/core
```

Полная документация: [Recap Engine Docs](../../apps/docs) (`pnpm docs:dev` из корня `recap-engine`).
