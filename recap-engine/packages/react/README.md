# @recap-engine/react

React Story Renderer: `<Recap>`, layouts, primitives, `registerScene`.

```bash
pnpm add @recap-engine/react @recap-engine/core
```

```ts
import '@recap-engine/react/styles.css'
```

Полная документация: [Recap Engine Docs](../../apps/docs) (`pnpm docs:dev` из корня `recap-engine`).
